/** Max width of the main conversation column (centered) */
export const CONVERSATION_MAX_W = "max-w-[min(86vw,44rem)]";
/** Shared horizontal padding for the message list, composer, and top-bar banner (keeps the column off the window edge when the sidebar is widened) */
export const CONVERSATION_GUTTER_X = "px-4 sm:px-5";
export const CONVERSATION_GUTTER_NEG_X = "-mx-4 sm:-mx-5";
/** Scroll-bed fallback before the composer-dock height is measured (previously 12rem) */
export const CONVERSATION_COMPOSER_SCROLL_BED_FALLBACK_PX = 192;
/** Extra scrollable whitespace between the last message and the composer overlay */
export const CONVERSATION_SCROLL_BED_EXTRA_PX = 48;
