---
title: 核准
description: 預設、自動核准與略過核准。每個層級仍會攔截哪些操作。
---

每個高風險工具呼叫都可以等待您核准。Spirit 有三個核准層級。Desktop 顯示為 **預設核准**、**自動核准** 和 **略過核准**。CLI 使用 `/approval` 或 `-a`。

| 層級 | ID | 行為 |
| --- | --- | --- |
| 預設核准 | `default` | 您自行核准檔案編輯、Shell 及其他高風險工具。 |
| 自動核准 | `auto-approval` | 輕量級模型會審查高風險呼叫。除非該審查（或鉤子）要求，否則不會提示您。 |
| 略過核准 | `bypass-approval` | 高風險工具執行時不會顯示核准提示。 |

## 略過核准不會跳過的項目

`bypass-approval` 仍會阻止以下情況：

- [ask_questions](./ask-questions.mdx)
- 需要問題的擴充功能流程
- 在 `preToolUse` 上傳回 `ask` 的鉤子

自動核准也無法覆寫鉤子的 `ask`。

## 何時使用哪一種

在您看過智慧體做出幾次好的編輯之前，請保持**預設**。當您想要速度但仍想要二次檢查時，請使用**自動核准**。僅在一次性分支或您正在觀看的任務上使用**略過**。

無頭模式的 `spirit -p` 若呼叫需要核准則會失敗。請傳入 `-a auto-approval` 或 `-a bypass-approval`，或改用 TUI。
