---
title: Подтверждения
description: Обычное, автоматическое подтверждение и обход подтверждения. Что каждый уровень всё ещё останавливает.
---

Каждый рискованный вызов инструмента может ожидать вашего подтверждения. У Spirit есть три уровня подтверждения. Desktop показывает их как **Обычное подтверждение**, **Автоматическое подтверждение** и **Обход подтверждения**. CLI использует `/approval` или `-a`.

| Уровень                      | Id                | Поведение                                                                                                         |
| ---------------------------- | ----------------- | ----------------------------------------------------------------------------------------------------------------- |
| Обычное подтверждение        | `default`         | Вы сами подтверждаете изменения файлов, shell и другие высокорисковые инструменты.                                |
| Автоматическое подтверждение | `auto-approval`   | Лёгкая модель проверяет высокорисковые вызовы. Вас не спрашивают, если только эта проверка (или хук) не запросит. |
| Обход подтверждения          | `bypass-approval` | Высокорисковые инструменты работают без запроса подтверждения.                                                    |

## Что обход не пропускает

`bypass-approval` всё ещё останавливается для:

- [ask_questions](./ask-questions.mdx)
- Потоков расширений, которым нужны вопросы
- Хука, который возвращает `ask` на `preToolUse`

Автоматическое подтверждение также не может переопределить хук `ask`.

## Когда что использовать

Оставайтесь на **обычном** пока не увидите, что агент сделал несколько хороших правок. Используйте **автоматическое подтверждение**, когда нужна скорость, но вы всё ещё хотите вторую проверку. Используйте **обход** только на временной ветке или для задачи, за которой вы наблюдаете.

Автономный `spirit -p` завершается с ошибкой, если вызову нужно подтверждение. Передайте `-a auto-approval` или `-a bypass-approval`, либо используйте TUI.
