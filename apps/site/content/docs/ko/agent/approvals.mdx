---
title: 승인
description: 기본, 자동 승인, 우회 승인. 각 수준에서 여전히 중지되는 항목.
---

모든 위험한 도구 호출은 사용자의 승인을 기다릴 수 있습니다. Spirit에는 세 가지 승인 수준이 있습니다. Desktop에서는 **Default Approval**, **Auto Approval**, **Bypass Approval**로 표시됩니다. CLI에서는 `/approval` 또는 `-a`를 사용합니다.

| 수준 | ID | 동작 |
| --- | --- | --- |
| 기본 승인 | `default` | 파일 편집, 셸 및 기타 고위험 도구를 직접 승인합니다. |
| 자동 승인 | `auto-approval` | 경량 모델이 고위험 호출을 검토합니다. 해당 검토(또는 후크)가 요청하지 않는 한 프롬프트가 표시되지 않습니다. |
| 우회 승인 | `bypass-approval` | 고위험 도구가 승인 프롬프트 없이 실행됩니다. |

## 우회가 건너뛰지 않는 항목

`bypass-approval`도 다음 경우에는 중지됩니다:

- [ask_questions](./ask-questions.mdx)
- 질문이 필요한 확장 프로그램 흐름
- `preToolUse`에서 `ask`를 반환하는 후크

자동 승인 역시 후크의 `ask`를 재정의할 수 없습니다.

## 언제 무엇을 사용해야 하는가

에이전트가 몇 번의 올바른 편집을 수행하는 것을 확인할 때까지 **기본**으로 유지하세요. 속도를 원하지만 두 번째 확인을 원할 때 **자동 승인**을 사용하세요. **우회**는 임시 브랜치 또는 지켜보고 있는 작업에서만 사용하세요.

헤드리스 `spirit -p`는 호출에 승인이 필요한 경우 실패합니다. `-a auto-approval` 또는 `-a bypass-approval`을 전달하거나 TUI를 사용하세요.
