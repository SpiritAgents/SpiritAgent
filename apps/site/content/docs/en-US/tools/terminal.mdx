---
title: Terminal
description: The agent runs shell commands with the shell tool. Desktop also has an Electron PTY panel.
---

The agent runs commands with `shell`. It sends a `reason` and a `command`. On default [approval](../agent/approvals.mdx) you see the command before it runs.

Ask mode cannot call `shell`.

## Desktop panel

Desktop (Electron only) has a **Shell** workspace tab with an embedded PTY. That panel is for you. The agent still goes through `shell` and approval; it does not type into your PTY.

CLI has no embedded PTY panel. Commands still run through the same `shell` tool on the shared daemon.

Override the Desktop PTY shell with `SPIRIT_TERMINAL_SHELL` if you need a specific binary.
