---
title: Genehmigungen
description: Standard, automatische Genehmigung und Genehmigung umgehen. Was jede Stufe noch stoppt.
---

Jeder riskante Tool-Aufruf kann auf Sie warten. Spirit hat drei Genehmigungsstufen. Desktop zeigt sie als **Standard-Genehmigung**, **Automatische Genehmigung** und **Genehmigung umgehen**. Die CLI verwendet `/approval` oder `-a`.

| Stufe | Id | Verhalten |
| --- | --- | --- |
| Standard-Genehmigung | `default` | Sie genehmigen Dateibearbeitungen, Shell und andere risikoreiche Tools selbst. |
| Automatische Genehmigung | `auto-approval` | Ein leichtgewichtiges Modell überprüft risikoreiche Aufrufe. Sie werden nur aufgefordert, wenn diese Überprüfung (oder ein Hook) danach fragt. |
| Genehmigung umgehen | `bypass-approval` | Risikoreiche Tools werden ohne Genehmigungsaufforderung ausgeführt. |

## Was die Umgehung nicht überspringt

`bypass-approval` stoppt weiterhin für:

- [ask_questions](./ask-questions.mdx)
- Erweiterungsabläufe, die Fragen benötigen
- Einen Hook, der bei `preToolUse` `ask` zurückgibt

Die automatische Genehmigung kann auch ein Hook-`ask` nicht außer Kraft setzen.

## Wann welche verwenden

Bleiben Sie bei **Standard**, bis Sie gesehen haben, dass der Agent ein paar gute Bearbeitungen vornimmt. Verwenden Sie **automatische Genehmigung**, wenn Sie Geschwindigkeit wünschen, aber dennoch eine zweite Prüfung möchten. Verwenden Sie **Umgehen** nur auf einem Wegwerf-Zweig oder bei einer Aufgabe, die Sie beobachten.

Headless `spirit -p` schlägt fehl, wenn ein Aufruf eine Genehmigung benötigt. Übergeben Sie `-a auto-approval` oder `-a bypass-approval`, oder verwenden Sie die TUI.
