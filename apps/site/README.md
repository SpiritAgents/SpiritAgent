# spirit.fast

Marketing site for Spirit Agent.

Built with Next.js and deployed on Vercel. Preview: https://next.spirit.fast

## Development

**Requirements:** Node.js 24+ (see `.nvmrc`), pnpm 10+ (enable via `corepack enable`).

```bash
corepack enable
pnpm install
pnpm run dev
```
